openapi: 3.2.0
info:
  title: LearnWorlds Progress API
  description: The LearnWorlds API (v2) is a REST API for programmatically managing an online school on the LearnWorlds course / LMS platform.
  version: '2.0'
  contact:
    name: LearnWorlds
    url: https://www.learnworlds.dev
  license:
    name: Proprietary
    url: https://www.learnworlds.com/terms-of-service/
servers:
- url: https://{school}.learnworlds.com/admin/api/v2
  description: Per-school API base URL
  variables:
    school:
      default: yourschool
      description: The subdomain / school identifier of your LearnWorlds academy.
security:
- oauth2ClientCredentials: []
  lwClient: []
tags:
- name: Progress
  description: Read per-user course progress and completion.
paths:
  /users/{id}/progress:
    parameters:
    - $ref: '#/components/parameters/UserId'
    get:
      operationId: getUserProgress
      tags:
      - Progress
      summary: Get a user's progress
      description: Returns the user's progress and completion status across the courses they are enrolled in.
      responses:
        '200':
          description: The user's course progress.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgressList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  schemas:
    ProgressList:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Progress'
        meta:
          $ref: '#/components/schemas/Pagination'
    Error:
      type: object
      properties:
        success:
          type: boolean
          example: false
        errors:
          type: array
          items:
            type: object
            properties:
              code:
                type: string
              message:
                type: string
    Progress:
      type: object
      properties:
        course_id:
          type: string
        user_id:
          type: string
        progress_rate:
          type: number
          description: Completion percentage for the course (0-100).
        status:
          type: string
          enum:
          - not_started
          - in_progress
          - completed
        completed_units:
          type: integer
        total_units:
          type: integer
    Pagination:
      type: object
      properties:
        page:
          type: integer
        total_items:
          type: integer
        items_per_page:
          type: integer
        total_pages:
          type: integer
  responses:
    NotFound:
      description: The requested resource was not found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Missing or invalid access token or Lw-Client header.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  parameters:
    UserId:
      name: id
      in: path
      required: true
      description: The user identifier (LearnWorlds user id or email).
      schema:
        type: string
  securitySchemes:
    oauth2ClientCredentials:
      type: oauth2
      description: 'OAuth2 client credentials flow. Exchange your client_id and client_secret for a bearer access token at the school token endpoint, then pass it as Authorization: Bearer ACCESS_TOKEN.'
      flows:
        clientCredentials:
          tokenUrl: https://yourschool.learnworlds.com/admin/api/oauth2/access_token
          scopes: {}
    lwClient:
      type: apiKey
      in: header
      name: Lw-Client
      description: Client identifier header sent on every request alongside the bearer token. Its value is your API client_id.
